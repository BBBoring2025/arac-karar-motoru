=== /api/early-access production probes (post-deploy, pre-migration) ===

Migration 003 NOT YET APPLIED — tests below prove the route is wired
but the DB table doesn't exist.

PROBE 1 — valid payload (expect 500 db_error PGRST205):
$ curl -s -X POST "https://arac-karar-motoru.vercel.app/api/early-access" \
    -H "Content-Type: application/json" \
    -d '{"ad":"Sprint D Test","email":"sprintd-pre-migration@example.com","ilgi":"genel","not_metni":"pre-migration probe"}'

{"ok":false,"error":"db_error","code":"PGRST205"}
HTTP_CODE: 500

Analysis:
- Route handler deployed ✓
- Validation passed ✓
- Insert attempt reached Supabase ✓
- Supabase returned "relation does not exist" (PGRST205) ✓
- Error code surfaced without leaking .message ✓

PROBE 2 — invalid payload (expect 400 validation error):
$ curl -s -X POST "https://arac-karar-motoru.vercel.app/api/early-access" \
    -H "Content-Type: application/json" \
    -d '{"ad":"X","email":"invalid-email","ilgi":"invalid","not_metni":""}'

{"ok":false,"error":"ad_too_short"}
HTTP_CODE: 400

Analysis:
- Validation short-circuits before DB call ✓
- First-found error code returned ("ad_too_short" is first failure) ✓
- No secret leak ✓

=== Post-migration retest checklist ===
After user applies migration 003 via Supabase Dashboard SQL Editor:

1. POST valid payload → expect {"ok":true,"id":<number>}
2. SELECT COUNT(*) FROM erken_erisim WHERE email = 'sprintd-test@example.com' → expect 1
3. DELETE the test row to avoid polluting prod
4. GET /api/admin/early-access via authenticated admin session → expect 200 with entries array
